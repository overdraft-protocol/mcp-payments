/** Stable identifier for the x402-evm-exact rail. Kept in its own module so
 *  helpers can reference it without importing the rail factory (avoids a cycle). */
export const RAIL_ID = 'x402-evm-exact';
